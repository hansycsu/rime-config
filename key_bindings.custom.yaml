patch:
  numbered_mode_switch:
    __append:
      - { when: always, accept: Shift+space, toggle: full_shape }
      - { when: composing, accept: Control+1, toggle: zh_tw }
      - { when: composing, accept: Control+2, toggle: zh_cn }
